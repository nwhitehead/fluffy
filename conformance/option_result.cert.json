{
  "item": "make_some",
  "level": "L3",
  "contract_quality": {
    "tautology": false,
    "vacuous_precondition": false
  },
  "effects": ["pure"],
  "slag": false,
  "note": "Stable-subset oracle (goal.md R-CHAR-3): item/level/tautology/vacuous_precondition/effects/slag are hand-derived from .design/basis/09-option-result.md AC-1 (built-in Some construction + the payload-in-contract spec-match-in-ens) + fluffy-design.md §6 ladder, and are oracle-compared. mutants_killed + solver_time_ms are tool-computed + EXCLUDED (oracle_subset, §5.3). `make_some` proves L3: `Some(5)` constructs via the seeded built-in `Option` variant registry, and `ens match result { Some(v) => v == 5, None => true }` projects the Some payload through the spec-match-in-ens; the Some-arm predicate `v == 5` is mutation-killable (a `Some(0)` mutant violates it), so the contract is non-vacuous. fx pure (constructing an `Option` value does not allocate). The sibling items in option_result.th — `small` (Some AND None, both arms meaningful), `ok_seven` (`Ok(7)` + the two-arg `Result<u64, ParseErr>` type), `checked` (Ok AND Err) — all certify L3 likewise (full coverage in forge/tests/option_result_conformance.rs)."
}
