{
  "item": "deposit",
  "level": "L3",
  "contract_quality": {
    "tautology": false,
    "vacuous_precondition": false
  },
  "effects": ["pure"],
  "slag": false,
  "note": "Stable-subset oracle (goal.md R-CHAR-3): item/level/tautology/vacuous_precondition/effects/slag are hand-derived from .design/basis/01-adts.md (REQ-1 struct+inv, REQ-8 struct->Verus + invariant-enforced) + fluffy-design.md, and are oracle-compared. mutants_killed + solver_time_ms are tool-computed + EXCLUDED (oracle_subset, §5.3 nondeterminism precedent — same as the per-cert ratios). `deposit` proves L3: ens `result.balance == a.balance + amount` is discharged given req `a.balance + amount <= 1_000_000` and the Account type-invariant `balance <= 1_000_000` (Verus enforces it at construction of the returned StructLit). fx pure — constructing a `struct` value does NOT allocate (only `Box` carries `fx alloc`). Non-vacuous: the ens is a real field relation, the req is satisfiable."
}
